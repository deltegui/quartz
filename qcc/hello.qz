import "stdio";
import "stdconv";

class Memory {
    pub fn increment() {
        self.read();
    }

    pub fn read() {
        println("Read");
    }
}

var mem = new Memory();

mem.increment();

