import 'stdio';
import 'stdconv';

for (;;) {
    print("Dime el salario anual: ");
    var in = readstr();
    println("He leido esto: " + in);
    var num = ston(in);
    if (num == 0) {
        println("No era un numero");
        continue;
    }
    println("El salario por mes es: " + ntos(num/12));
    break;
}

