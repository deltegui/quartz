import 'stdio';
import "stdconv";
import 'stdtime';

fn salut(lang: String): (String): Void {
    fn to(name: String) {
        println(name + lang);
    }
    return to;
}

var spanish = salut('hola');
spanish('diego');