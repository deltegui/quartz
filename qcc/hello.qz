import 'stdio';
import 'stdconv';

class Macha {
    var name: String;
    var age: Number;

    pub fn init(name: String, age: Number) {
        println("Hello from here putos");
    }

    pub fn hello() {
        println("Hello");
    }
}

var diego = new Macha('diego', 22);
diego.hello();
println(ntos(diego.age));
