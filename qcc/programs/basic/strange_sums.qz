import 'stdio';
import 'stdconv';

println(ntos(-1));

// This thing also works in Java.
println(ntos(1+-1));

/**
This works like python but unlike Java.
The parser thinks that these + signs are
unary and affects the last one.
*/
println(ntos(1++1));
println(ntos(1++++++++++++1));