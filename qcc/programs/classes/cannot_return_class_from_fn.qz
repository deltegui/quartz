import 'stdio';
import 'stdconv';

class Human {}

fn do(): Human {
    class Human {
        var name: String;
        var age: Number;

        pub fn init(name: String, age: Number) {
            self.name = name;
            self.age = age;
        }

        pub fn say_hi() {
            println("Hello, im " + self.name + " and im " + ntos(self.age) + " years old!");
        }
    }
    return new Human('diego', 12);
}

do()();