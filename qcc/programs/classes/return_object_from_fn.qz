import 'stdio';

class Demo {
    var thing_to_print: String;

    pub fn init(str: String) {
        self.thing_to_print = str;
    }

    pub fn print() {
        println(self.str);
    }
}

fn rtn_obj(str: String) {
    return new Demo(str);
}

rtn_obj("HOLI!").print();
