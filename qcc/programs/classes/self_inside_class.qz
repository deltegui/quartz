import 'stdio';
import 'stdconv';

class Human {
    var name: String;
    var age: Number;

    pub fn init(name: String, age: Number) {
        self.name = name;
        self.age = age;
    }

    pub fn salut() {
        println("Hello Im " + self.name + " and Im " + ntos(self.age) + " years old!");
    }
}

var manolo = new Human("Manolo", 33);
manolo.salut();

var sandra = new Human("Sandra", 21);
sandra.salut();
