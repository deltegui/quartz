import 'stdio';
import 'stdconv';

fn hola(): (Number): Void {
    fn mama(a: Number): Void {
        println(ntos(a));
    }
    return mama;
}

var a: (Number): Void = hola();
a(5);

var b = hola();
b(6);