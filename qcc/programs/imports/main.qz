import 'stdio';
import '../programs/imports/other.qz';
import '../programs/imports/name.qz';

var first: Name = generate_name(0);
println(first);

var infer = generate_name(1);
println(infer);

var str: String = generate_name(2);
println(str);

var other: Name = generate_name(3);
println(other);