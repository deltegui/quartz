import 'stdio';

typedef Name = String;

fn generate_name(index: Number): Name {
    if (index == 0) return 'Javier';
    if (index == 1) return 'Diego';
    if (index == 2) return 'Maria';
    return 'Antonio';
}

var first: Name = generate_name(0);
println(first);

var infer = generate_name(1);
println(infer);

var str: String = generate_name(2);
println(str);

var other: Name = generate_name(3);
println(other);
